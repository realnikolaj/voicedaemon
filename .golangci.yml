version: "2"
run:
  build-tags:
    - noapm
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close
formatters:
  enable:
    - gofmt
